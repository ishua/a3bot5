version: "3.1"

networks:
  dnet:
    name: dnetmy
    driver: bridge

services:
  tbot:
    image: tbot
    build:
      context: ./
      dockerfile: ./tbot/Dockerfile
    networks:
      - dnet
    volumes:
      - ./tbot/conf:/conf